# Cursor Rules Package

This directory holds the Cursor rules packaging for `7-day-mobile-and-web-design`.

It contains project-level rules or prompt files adapted from the shared core.

Primary rule file: `rules/7-day-mobile-and-web-design.mdc`.
